# Boiler Mobile App - Environment Configuration
# Copy this file to .env.local and fill in your values

# Server IP/Hostname where Docker services are running
# Examples:
#   - localhost (for local development)
#   - 192.168.1.100 (LAN IP)
#   - your-server.com (domain name)
EXPO_PUBLIC_SERVER_HOST=localhost
